# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Project rules for this app live in the repository root `CLAUDE.md` and in `.claude/rules/mobile.md` and `.claude/rules/layout.md`. They apply to every agent.
