// Placeholder. Screens (report queue, photo queue, config editor) arrive with M4.
// English only by decision (TD-17); the component library choice is open (rules/admin.md).
export function App() {
  return (
    <main>
      <h1>Kuutti admin</h1>
      <p>Moderation panel. Nothing to moderate yet.</p>
    </main>
  );
}
