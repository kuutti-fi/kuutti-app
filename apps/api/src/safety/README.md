# safety

Reports, blocks, sanctions, the audit log. TD-5, TD-6. The audit log (`audit.ts`, #49, ADR-006) is the first piece: append-only by trigger, one row per staff action or view; the rest arrives with M4.
