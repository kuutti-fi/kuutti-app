# Agent instructions

All instructions for coding agents in this repository live in `CLAUDE.md` and, per package, in `.claude/rules/`. They apply to every agent, not only Claude. Read `CLAUDE.md` first.
