{
  "name": "@kuutti/schema",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "zod contracts shared by api, mobile and admin; the research event registry lives here too.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.6.0"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
