# @kuutti/schema

Single source of contracts: route inputs and outputs as zod schemas, and the research event registry. zod is the only dependency because React Native imports this package. Rules: `.claude/rules/schema.md`.
