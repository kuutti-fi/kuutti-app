# @kuutti/db

Drizzle ORM schema in `src/schema`, migrations generated into `drizzle/` by drizzle-kit and committed as SQL, a migrate runner the API entrypoint calls under an advisory lock, and the seed. Contents: issue #4. Rules: `.claude/rules/db.md`.
