name: Auto-approve own pull requests

# Runs from main only (schedule and manual dispatch never run a pull request's
# copy of this file), so a contributor cannot edit the condition in their own
# pull request and approve themselves. It reads nothing from a pull request but
# its author, draft state and head repository. Latency: GitHub's schedule, a
# few minutes. Needs "Allow GitHub Actions to create and approve pull requests".
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  approve:
    name: approve the maintainer's open pull requests
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Approve what the maintainer authored from this repository and nobody approved yet
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          gh pr list --repo "$REPO" --author superseacat --state open \
            --json number,isDraft,reviewDecision,headRepositoryOwner \
            --jq '.[] | select(.isDraft == false and .reviewDecision != "APPROVED" and .headRepositoryOwner.login == "kuutti-fi") | .number' |
          while read -r n; do
            gh pr review --repo "$REPO" --approve \
              --body "Approved automatically: authored by the maintainer from this repository (#8, auto-approve.yml)." "$n"
          done
